# CI — phase1 wrapper (SP-027). Replaces the pre-phase1 self-contained CI
# (dead on the org plane since 2026-02; preserved in git history).
# Lane: LiquidMovz/ci-library verify-node-pnpm.yml (mirror plane, SHA-pinned).
# Source: Forge platform-ci kit.
name: CI

on:
  workflow_dispatch:
  push:
    branches:
      - master

permissions:
  contents: read

jobs:
  verify:
    uses: LiquidMovz/ci-library/.github/workflows/verify-node-pnpm.yml@fca3b70a8fd68cfc2da1576388698f5a26e19715
    with:
      node-version: "24"
